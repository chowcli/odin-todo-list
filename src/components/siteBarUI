const navBarUI = (() => {
  const siteBar = document.querySelector("site-bar");

  siteBar.addEventListener("click", event => {
    const { target } = event;

    if (target.closest(".add-new-project")) {
      
    }
  });
})();
